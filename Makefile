check-config:
    # ansible running some yaml checks

install:
    # install all utils based on the OS

build-config:
    # merge all ansible configurations into one

deploy:
    # run ansible command line

list-hosts:
    # list all hosts
